// scoreboard as an object
var scores = {
	playerOneScore: 0,
	playerTwoScore: 0
};

var currentPlayerOneScore = scores.playerOneScore +=1
var currentPlayerTwoScore = scores.playerTwoScore +=1
var currentPlayerScores = scores
console.log(scores)
console.log (currentPlayerScores)

// scoreboard as an array
var scores = [ 20, 30 ];
var playerOneScore = scores[0];
var playerTwoScore = scores[1];
var currentPlayerScores = scores

console.log(scores)
console.log (currentPlayerScores)